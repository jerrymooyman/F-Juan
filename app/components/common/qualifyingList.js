'use strict'

import React from 'react';

class QualifyingList extends React.Component {

		// createListItems() {
		// 	if (!this.props.data) return ''

		//     const style = {
		//       	padding: 0
		//     };

		// 	return this.props.data.map( row => {
		// 			let pointsText = row.points > 0 ? '     for ' + row.points + ' points' : ''
		//     		return (
		//     			''
		//     		)
		// }

	render() {
		return (
			<div>
			</div>
		);
	}
}

export default QualifyingList
